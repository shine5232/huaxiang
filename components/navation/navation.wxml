<view class="nav" style="height:{{navigationBarHeight}}">
  <view style="height:{{statusBarHeight}}"></view>
  <view class="title-container">
    <van-icon name="arrow-left" size="50rpx" catchtap="goBack" wx:if="{{hidden}}" />
    <view>{{title}}</view>
  </view>
</view>